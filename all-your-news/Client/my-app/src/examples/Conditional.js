import React from "react";

function Conditional(props){
    return (
        <div>
            <h1>Some coool stuff!!</h1>
        </div>
    )
    //{props.isLoading ? <h1>Loading ...</h1> : <h1>Some coool stuff!!</h1>}
}

export default Conditional;